# ichuhai 产品完整规划

## 1. 产品目标

ichuhai 是一个虚拟数字商品商城，面向全球用户销售 Discord Nitro、Spotify Premium、YouTube Premium、Steam Wallet、Microsoft 365 等数字商品。产品核心目标是让用户快速浏览商品、进入详情页确认规格，以 USD 查看商品标价，并通过余额、USDT TRC20 或支付宝完成付款后获得自动或人工发货结果。法币金额仅作为参考价展示。

## 2. 本地交付范围

当前交付为可运行 MVP：

- 前台官网：首页、商品列表、商品详情、支付、订单完成、订单查询、FAQ、用户中心入口。
- 购买系统：动态商品、规格组选项、SKU USD 标价、库存状态、发货方式、支付方式、支付网络、法币参考价。
- 订单系统：订单创建、支付信息、订单状态、订单查询、完成页交付结果。
- 后台系统：商品/SKU 配置入口、订单管理、支付网络配置、发货方式与发货记录管理。
- 服务端 API：商品、详情、下单、支付信息、订单状态、订单查询、Telegram 登录、汇率、偏好设置、支付监听、标记支付、发货、后台查询。
- 数据持久化：本地 `data/db.json` 文件保存商品配置、订单和发货记录。

## 3. 页面规划

| 页面 | 路由 | 当前实现 |
| --- | --- | --- |
| 首页 | `#/` | 商品浏览、分类筛选、搜索、可售规格摘要与商品入口 |
| 商品列表 | `#/products` | 全商品列表、分类、搜索、排序、规格范围与 USD 起价 |
| 商品详情 | `#/product/:slug` | 商品主视觉、规格选择、数量、支付方式、USD 标价、后台详情说明、立即购买 |
| 购买确认 | `#/checkout` | 后置兼容页；主流程由商品详情页直接创建订单 |
| 支付页 | `#/pay/:orderId` | 状态步骤、钱包入口、二维码、地址、金额、确认支付 |
| 订单完成 | `#/order/:orderId/success` | 成功区、进度条、订单信息、交付内容、后续操作 |
| 订单查询 | `#/orders/lookup` | 订单号 + 邮箱或 Telegram 查询 |
| 用户中心 | `#/account` | Telegram 登录状态与订单列表入口 |
| 后台 | `#/admin` | 商品、订单、支付网络、发货管理 |
| FAQ | `#/faq` | 支付、发货、退款说明 |

## 4. 核心业务模型

商品由 `Product`、`ProductSku`、`PaymentNetwork`、`Order`、`DeliveryRecord` 组成。前端根据商品配置动态渲染规格组，不硬编码“套餐”逻辑。商品可以没有规格组，但必须至少有一个默认 SKU；没有规格时详情页不显示规格区块，首页直接展示单 SKU 标价。订单创建时保存商品和 SKU 快照，避免后台后续改价或改发货方式影响历史订单。

商品展示价格统一为 USD，字段可沿用历史 `priceUsdt` 命名作为兼容层，但前台与后台商品/SKU 管理对用户展示为 USD。用户选择的 CNY/USD/GBP/EUR/AUD/JPY/HKD/KRW 仅为参考折算价。最终支付方式支持余额支付、USDT TRC20 和支付宝；选择 USDT 时支付页展示实际 USDT 转账金额、网络、地址和倒计时。

## 5. 用户路径

1. 用户进入首页或商品列表页。
2. 系统默认显示 CNY 参考价，用户可切换 USD/CNY/GBP/EUR/AUD/JPY/HKD/KRW。
3. 首页商品卡展示可售规格范围和 USD 起价；0 数量分类不展示。
4. 用户进入商品详情页，选择地区、账号类型、周期、面额或其他后台配置的规格；无规格商品不显示规格选择区。
5. 用户选择数量和支付方式。未登录时余额支付显示“登录后可用”，不可直接使用。
6. 用户点击立即购买。未登录用户先进入登录页；已登录用户直接创建订单。
7. 余额足够时订单直接完成；选择 USDT 时进入支付页展示二维码、收款地址、金额、网络和倒计时；选择支付宝时进入对应支付执行流程。
8. 用户点击“我已完成支付”后，本地 API 模拟链上标记已支付并触发发货。
9. 订单完成页展示交付结果，订单可通过查询页或用户中心查回。

## 6. 后台规划

后台 MVP 包含：

- 商品与 SKU：查看商品、SKU 数量、USD 标价、进入编辑入口，支持本地 API 上下架商品、调整 SKU 和维护商品详情说明长文。
- 订单管理：查看订单状态、手动标记已支付、手动更新状态、手动补发。
- 支付配置：查看 TRON/ETH/BSC/BASE 网络、收款地址、确认数，支持启停网络和设置推荐网络；前台主支付方式为余额、USDT TRC20 和支付宝。
- 发货管理：按商品查看自动/手动/混合发货方式、库存来源入口、发货记录。

生产版建议继续扩展为：新增/编辑商品表单、SKU 组合生成器、库存导入、权限控制、支付监听配置、通知模板、发货失败重试。

购物车不属于当前主流程。前台主路径采用“商品列表/首页 -> 商品详情 -> 立即购买 -> 支付/完成”。购物车作为后续可选能力保留在产品规划中，当前不在顶部导航和购买按钮中展示。

## 7. 技术规划

当前本地 MVP 使用零依赖架构：

- `index.html` 承载应用入口。
- `styles.css` 复刻参考图视觉风格。
- `app.js` 实现前端路由、状态、交互与 API 调用。
- `server.mjs` 提供本地 API 与静态文件服务。
- `data/db.json` 保存本地业务数据。

生产版建议按 README 规划升级为 Next.js + PostgreSQL + Prisma + Redis + Worker/Cron + Telegram Bot + Email Service + 链上监听服务。

## 8. 生产化缺口

当前交付可本地实际运行和演示完整购买闭环，但以下能力必须依赖真实外部配置后才能生产上线：

- Telegram Bot Token 与域名绑定。
- USDT 链上监听服务和真实节点供应商。
- 正式收款地址和财务风控策略。
- 邮件服务账号与发信域名。
- PostgreSQL/Prisma 迁移与生产数据备份。
- 管理员身份认证与权限体系。
- HTTPS 域名、部署环境、日志与告警。
