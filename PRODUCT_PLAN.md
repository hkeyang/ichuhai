# ichuhai 产品完整规划

## 1. 产品目标

ichuhai 是一个虚拟数字商品商城，面向全球用户销售 Discord Nitro、Spotify Premium、YouTube Premium、Steam Wallet、Microsoft 365 等数字商品。产品核心目标是让用户快速选择商品、确认规格、使用 USDT 支付，并在链上确认后获得自动或人工发货结果。

## 2. 本地交付范围

当前交付为可运行 MVP：

- 前台官网：首页、商品列表、商品详情、购买确认、支付、订单完成、订单查询、FAQ、用户中心入口。
- 购买系统：动态商品、规格组选项、SKU 价格、库存状态、发货方式、支付网络、法币参考价。
- 订单系统：订单创建、支付信息、订单状态、订单查询、完成页交付结果。
- 后台系统：商品/SKU 配置入口、订单管理、支付网络配置、发货方式与发货记录管理。
- 服务端 API：商品、详情、下单、支付信息、订单状态、订单查询、Telegram 登录、汇率、偏好设置、支付监听、标记支付、发货、后台查询。
- 数据持久化：本地 `data/db.json` 文件保存商品配置、订单和发货记录。

## 3. 页面规划

| 页面 | 路由 | 当前实现 |
| --- | --- | --- |
| 首页 | `#/` | 左侧商品浏览与规格选择，右侧快速下单 |
| 商品列表 | `#/products` | 全商品列表、分类与搜索 UI |
| 商品详情 | `#/product/:slug` | 商品主视觉、规格、价格、订单摘要、说明、相关推荐 |
| 购买确认 | `#/checkout` | 步骤条、联系信息、支付网络、支付前确认、阅读确认 |
| 支付页 | `#/pay/:orderId` | 状态步骤、钱包入口、二维码、地址、金额、确认支付 |
| 订单完成 | `#/order/:orderId/success` | 成功区、进度条、订单信息、交付内容、后续操作 |
| 订单查询 | `#/orders/lookup` | 订单号 + 邮箱或 Telegram 查询 |
| 用户中心 | `#/account` | Telegram 登录状态与订单列表入口 |
| 后台 | `#/admin` | 商品、订单、支付网络、发货管理 |
| FAQ | `#/faq` | 支付、发货、退款说明 |

## 4. 核心业务模型

商品由 `Product`、`ProductSku`、`PaymentNetwork`、`Order`、`DeliveryRecord` 组成。前端根据商品配置动态渲染规格组，不硬编码“套餐”逻辑。订单创建时保存商品和 SKU 快照，避免后台后续改价或改发货方式影响历史订单。

## 5. 用户路径

1. 用户进入首页。
2. 系统默认显示 CNY 参考价，用户可切换 USD/CNY/GBP/EUR/AUD/JPY/HKD/KRW。
3. 用户选择商品和规格，右侧快速下单同步更新。
4. 用户可模拟 Telegram 登录，也可直接填写 Telegram 用户名和邮箱。
5. 用户选择 USDT 支付网络。
6. 用户进入购买确认页，勾选购买须知并创建订单。
7. 支付页展示二维码、收款地址、金额、网络和倒计时。
8. 用户点击“我已完成支付”后，本地 API 模拟链上标记已支付并触发发货。
9. 订单完成页展示交付结果，订单可通过查询页查回。

## 6. 后台规划

后台 MVP 包含：

- 商品与 SKU：查看商品、SKU 数量、进入编辑入口，支持本地 API 上下架商品和调整 SKU。
- 订单管理：查看订单状态、手动标记已支付、手动更新状态、手动补发。
- 支付配置：查看 TRON/ETH/BSC/BASE 网络、收款地址、确认数，支持启停网络和设置推荐网络。
- 发货管理：按商品查看自动/手动/混合发货方式、库存来源入口、发货记录。

生产版建议继续扩展为：新增/编辑商品表单、SKU 组合生成器、库存导入、权限控制、支付监听配置、通知模板、发货失败重试。

## 7. 技术规划

当前本地 MVP 使用零依赖架构：

- `index.html` 承载应用入口。
- `styles.css` 复刻参考图视觉风格。
- `app.js` 实现前端路由、状态、交互与 API 调用。
- `server.mjs` 提供本地 API 与静态文件服务。
- `data/db.json` 保存本地业务数据。

生产版建议按 README 规划升级为 Next.js + PostgreSQL + Prisma + Redis + Worker/Cron + Telegram Bot + Email Service + 链上监听服务。

## 8. 生产化缺口

当前交付可本地实际运行和演示完整购买闭环，但以下能力必须依赖真实外部配置后才能生产上线：

- Telegram Bot Token 与域名绑定。
- USDT 链上监听服务和真实节点供应商。
- 正式收款地址和财务风控策略。
- 邮件服务账号与发信域名。
- PostgreSQL/Prisma 迁移与生产数据备份。
- 管理员身份认证与权限体系。
- HTTPS 域名、部署环境、日志与告警。
